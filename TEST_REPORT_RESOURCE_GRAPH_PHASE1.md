# 资源关系子需求第一阶段测试报告

> 测试日期：2026-05-09  
> 测试范围：`RESOURCE_RELATION_REQUIREMENTS.md` 的 V1 基础版主链路  
> 测试目标：验证团队级资源画布、真实资源池、节点持久化、父子关系、Git 仓库节点、关系边和权限边界是否正确落地  
> 测试方式：真实后端接口测试 + 真实数据库注入 + 前后端构建验证  
> 原始结果：`.tmp/resource-graph-phase1-results.json`

---

## 1. 测试结论

本轮资源关系子需求 **通过第一阶段正确性测试**。

汇总结果：

- 总测试项：`26`
- 通过：`26`
- 失败：`0`
- 致命问题：`0`
- 高优先级问题：`0`

本轮确认以下主链路已打通：

- 团队级默认画布可获取
- 资源池返回真实任务 / 文档 / 成员 / 里程碑
- 里程碑、任务、文档、成员、Git 仓库节点可正式创建
- 任务节点可挂载到里程碑父节点
- 关系边可创建、修改、持久化
- 节点位置调整后可持久化
- 画布重新加载后父子关系和边仍存在
- 成员只读访问、Reader / Guest 禁止访问、跨租户拒绝、缺失租户头拒绝均成立
- 关键画布事件已进入审计日志

---

## 2. 测试环境

### 2.1 运行环境

- 后端：`http://localhost:3000/api/v1`
- 前端：`http://127.0.0.1:5173`
- 数据库：本地 PostgreSQL
- Prisma 迁移状态：`up to date`

### 2.2 构建验证

- `pnpm --filter @saas/backend build` 通过
- `pnpm --filter frontend build` 通过

---

## 3. 测试数据

本轮注入了一组唯一前缀的资源，用于避免与既有数据混淆：

- 前缀：`graph-phase1-1778312563504`
- 团队：`team1`
- 团队 ID：`2f2895e0-a781-4d45-b174-bf9fc8033ab1`
- 租户 ID：`98178ecd-7a75-4eb8-ba8b-a444c395a010`

注入资源：

- 任务：`graph-phase1-1778312563504-task`
- 文档目录：`graph-phase1-1778312563504-doc`
- 里程碑：`graph-phase1-1778312563504-milestone`
- Git 节点：`graph-phase1-1778312563504-repo`

测试账号：

- Owner/Admin：`2917781292@qq.com`
- Member：`ai.phase1.member@example.com`
- Reader：`ai.phase1.reader@example.com`
- Guest：`ai.phase1.guest@example.com`
- Outsider：`ai.phase1.outsider@example.com`

---

## 4. 详细结果

| 编号 | 测试项 | 结果 | 说明 |
|------|--------|------|------|
| RG-P1-001 | 所有测试账号可登录 | 通过 | Owner / Member / Reader / Guest / Outsider 均可获取有效 JWT |
| RG-P1-002 | Owner 可注入任务数据 | 通过 | 任务创建成功并带负责人、标签、团队归属 |
| RG-P1-003 | Owner 可注入文档目录数据 | 通过 | 文档目录创建成功 |
| RG-P1-004 | Owner 可注入里程碑数据 | 通过 | 里程碑创建成功并关联测试任务 |
| RG-P1-005 | Owner 获取团队画布 | 通过 | 返回团队默认画布，`canEdit=true` |
| RG-P1-006 | Owner 获取资源池 | 通过 | 资源池包含新建任务、文档、里程碑、成员 |
| RG-P1-007 | Owner 创建里程碑节点 | 通过 | 里程碑节点正式落库 |
| RG-P1-008 | Owner 创建任务节点并挂父里程碑 | 通过 | `parentNodeId` 正确保存 |
| RG-P1-009 | Owner 创建文档节点 | 通过 | 文档节点正式落库 |
| RG-P1-010 | Owner 创建成员节点 | 通过 | 成员节点正式落库 |
| RG-P1-011 | Owner 创建 Git 仓库节点 | 通过 | URL 正确保存到节点元数据 |
| RG-P1-012 | Owner 创建 `produces` 关系边 | 通过 | 任务到文档关系边创建成功 |
| RG-P1-013 | Owner 创建 `owned_by` 关系边 | 通过 | 任务到成员关系边创建成功 |
| RG-P1-014 | Owner 创建 `backed_by_repo` 关系边 | 通过 | 任务到 Git 仓库关系边创建成功 |
| RG-P1-015 | Owner 移动文档节点 | 通过 | 坐标更新成功 |
| RG-P1-016 | Owner 修改关系边类型 | 通过 | `produces -> references` 更新成功 |
| RG-P1-017 | 画布刷新后节点与边持久化 | 通过 | 父子关系、坐标、Git URL、关系边均保留 |
| RG-P1-018 | 旧资源中心式图接口兼容 | 通过 | `GET /graph/task/:taskId` 返回正常 |
| RG-P1-019 | 图谱审计事件存在 | 通过 | `graph.node.create` 等事件可在审计日志中查询 |
| RG-P1-020 | Member 可读画布但不可编辑 | 通过 | 读取成功，`canEdit=false` |
| RG-P1-021 | Member 可读资源池 | 通过 | 能看到权限范围内资源 |
| RG-P1-022 | Member 禁止创建节点 | 通过 | 返回 `403`，提示 `graph.update requires role LEADER` |
| RG-P1-023 | Reader 禁止访问画布 | 通过 | 返回 `403` |
| RG-P1-024 | Guest 禁止访问画布 | 通过 | 返回 `403` |
| RG-P1-025 | 跨租户访问被拒绝 | 通过 | Outsider 访问 `team1` 返回 `403 Cross-tenant team access is not allowed` |
| RG-P1-026 | 缺失 `X-Tenant-Id` 被拒绝 | 通过 | 返回 `400 X-Tenant-Id header is required` |

---

## 5. 需求映射结果

| 需求 ID | 验证结果 | 说明 |
|---------|----------|------|
| RR01 | 通过 | 已形成里程碑 / 任务 / 文档 / 成员 / Git 仓库统一节点模型 |
| RR02 | 通过 | 已存在团队级默认画布，可加载 |
| RR03 | 通过 | 节点创建、移动和坐标持久化成立 |
| RR04 | 通过 | 里程碑父子关系可建立并持久化 |
| RR05 | 通过 | 关系边可创建、更新、持久化 |
| RR06 | 通过 | Git 仓库节点可创建并保存 URL |
| RR07 | 部分验证 | 后端节点数据已具备跳转信息，但本轮未做浏览器级双击跳转点测 |
| RR08 | 通过 | 画布和资源池权限边界符合预期 |
| RR09 | 通过 | 关键图谱事件可进入审计日志；本轮验证了 JSON 导出接口数据基础，未做图片导出点测 |
| RR10 | 未覆盖 | V2 自动关系不在本轮范围内 |

---

## 6. 未覆盖项与残余风险

本轮未覆盖以下内容：

- 浏览器内真实拖拽交互的点测
- 节点双击跳转任务 / 文档 / 成员 / Git URL 的前端联动点测
- 图谱图片导出视觉正确性
- 多画布能力
- 批量 `save canvas` 路径
- 自动关系生成

残余风险：

- 现有 `team1` 已有历史图谱数据，本轮测试是在其基础上新增节点和边完成的，因此“空白新团队首开画布体验”尚未单独验证。
- 本轮以接口正确性为主，没有覆盖 React Flow 细节交互中的浏览器差异。

---

## 7. 结论建议

资源关系子需求目前已经具备进入第二阶段测试的条件。建议后续按以下顺序推进：

1. 做第二阶段回归与边界测试
2. 单独补浏览器级拖拽 / 双击跳转 / 导出点测
3. 如果要继续演进，再进入多画布、自动布局和自动关系能力
